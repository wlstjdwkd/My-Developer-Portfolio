version: "3"
services:
  jenkins:
    build:
      context: .
      dockerfile: dockerfile/Dockerfile.jenkins
    ports:
      - "8080:8080"
      - "50000:50000"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
    user: root
    networks:
      - portfolio

  react-app:
    build:
      context: .
      dockerfile: dockerfile/Dockerfile.react
    ports:
      - "80:80"

networks:
  portfolio:
    driver: bridge

volumes:
  jenkins_home:
